# Local Runs

Training checkpoints, TensorBoard logs, metrics, component CSVs, and review
catalogs can live here during local runs.

This directory is ignored by Git because run artifacts are generated and often
large.
